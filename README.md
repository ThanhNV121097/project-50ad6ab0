# project-50ad6ab0
A single page that shows the text "Hello Word" centred on the screen, horizontally and vertically. The text is not hardcoded in the frontend: it is stored in one row of a PostgreSQL table and served to the page by the backend. Nothing else — no navigation, no styling beyond centring, no other pages.
